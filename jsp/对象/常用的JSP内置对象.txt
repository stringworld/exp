request: 相应客户端，把数据提交出去，包含所有的请求的信息(有关请求的）

response：主要将JSP容器处理后的结果传回客户端（响应）

session：表示目前个别的用户会话的状态，用来识别每个用户，内存的一部分

application：主要用于取得或更改servlet的设定（全局的）

cookie：是Web服务器保存在用户硬盘上的一段文本。Cookie允许一个Web站点在用户电脑上保存信息并且随后再取回它。
    举例来说，一个Web站点可能会为每一个访问者产生一个唯一的ID，然后以Cookie文件的形式保存在每个用户的机器上。
