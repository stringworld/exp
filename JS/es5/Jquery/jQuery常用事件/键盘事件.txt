键盘事件可以分为2类：
	直接对键盘按键给出响应的事件（keyup和keydown）
	对文本输入给出响应的事件（keypress）

实践经验：如果想知道用户按了那个按键，应该侦听keyup和keydown事件，如果想知道用户输入的是什么字符，应该侦听keypress事件。


键盘事件的目标：是当前拥有键盘焦点的元素。

元素的焦点可能会在一下情况下转移：包括单机鼠标和按下Tab键

注意：
	并不是所有的元素都可以获得焦点，只有在那些默认的情况下具有键盘驱动行为的元素，如表单字段，链接，以及指定了tabIndex属性的元素才可以获得焦点。