<payments>
	<payment>
		<code>alipay</code>
		<name>支付宝</name>
		<description>全球领先的独立第三方支付平台,致力于为广大用户提供安全快速的电子支付</description>
		<attr>
			<type>input</type>
			<show>支付宝账户</show>
			<name>username</name>
		</attr>
		<attr>
			<type>list</type>
			<show>选择支付类型</show>
			<name>type</name>
			<option>
				<value>1</value>
				<show>标准双接口</show>
			</option>
			<option>
				<value>2</value>
				<show>单向接口</show>
			</option>
		</attr>
	</payment>
</payments>

$xml=simplexml_load_file(Gc::$url_base."module/plugins/pay/payments.xml");
foreach($xml->payment as $payment){
	echo $payment->code.",".$payment->name.",".$payment->description."<br/>";
	foreach($payment->attr as $attr){
		echo $attr->type.",".$attr->show.",".$attr->name."<br/>";
		if($attr->type=="list"){
			foreach($attr->option as $option){
				echo $option->value.",".$option->show."<br/>";
			}
		}
	}
}