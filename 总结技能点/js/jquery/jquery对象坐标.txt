（body属性设置margin :0;padding:0;）

获取页面某一元素的绝对X,Y坐标，可以用offset()方法：
var X = $('#DivID').offset().top;
var Y = $('#DivID').offset().left;

获取相对(父元素)位置:
var X = $('#DivID').position().top;
var Y = $('#DivID').position().left;